name: Test

on:   
  push:
    branches: 
      - master
      - release/*
      
jobs:
  test:
    needs: build
    runs-on:  ubuntu-latest
    steps:
      - name: test
        run: build/bin/net-test
        timeout-minutes: 5
